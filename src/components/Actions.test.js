//TODO: Write Tests for Actions

//test to make sure tests are working - delete once better tests written
describe('basic math', () => {
  it('returns basic math equation', () => {
    expect(2).toEqual(2);
  })
});